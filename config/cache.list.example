# Statements the transparent cache is allowed to serve from Valkey.
# Exact byte match against the statement text on the wire.
#
# Copy this to cache.list (gitignored) and list your own queries.
#
#   cp config/cache.list.example config/cache.list

SELECT * FROM my_table WHERE id = 1
