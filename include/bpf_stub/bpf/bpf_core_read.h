#pragma once
/*
 * IDE-indexing stub. The BPF programs include the real bpf_core_read.h
 * conventionally but does not use any BPF_CORE_READ macro (plain field
 * access is used throughout instead), so an empty file satisfies the
 * #include with nothing further needed. See bpf_helpers.h in this directory
 * for why this tree exists at all.
 */
#ifndef VALKEY_EBPF_BPF_CORE_READ_STUB_H
#define VALKEY_EBPF_BPF_CORE_READ_STUB_H
#endif /* VALKEY_EBPF_BPF_CORE_READ_STUB_H */
